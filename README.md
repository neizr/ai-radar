# AI Radar

A modern AI discovery + ranking site built with Astro 7 + React 19.

## Run locally

```bash
npm install
npm run dev
```

## Build for Vercel

```bash
npm run build
```

The project is static by default and can be deployed directly to Vercel.

## Next monetization layer

- Replace demo tool data with a database/API.
- Add real affiliate URLs per tool.
- Add `/tool/[slug]` comparison/review pages.
- Add newsletter capture (Resend/ConvertKit/etc.).
- Add sponsored placements with a clearly marked "Sponsored" label.
- Add analytics and outbound-click tracking.
